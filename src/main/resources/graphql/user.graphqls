type User {
    id: ID
    name: String
    email: String
    description: String
}

input UserInput {
    id: ID
    name: String
    email: String
    description: String
}

type Query {
    users(where: UserInput): [User]
    users2(where: UserInput): [User]
}
