type ApplianceDto {
    id: ID
    name: String
    brand: String
    price: Int
    description: String
}

input ApplianceInput {
    id: ID
    name: String
    brand: String
    price: Int
    description: String
}

extend type Query {
    appliances(where: ApplianceInput): [ApplianceDto]
}